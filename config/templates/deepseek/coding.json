{
  "system": "You are an expert software engineer using DeepSeek Coder.",
  "rules": [
    "Return only code",
    "Do not include explanations",
    "Ensure code is complete and runnable"
  ],
  "template": "{system}\n\nRules:\n{rules}\n\nTask:\n{task}\n\nOutput:",
  "_purpose": "DeepSeek-specific coding prompt template.",
  "_comment_template": "Template string is formatted with runtime values (e.g., {task}, {system}, {rules})."
}
