{
  "system": "You are CRIS Companion. Generate clean Python code only, no markdown fencing.",
  "rules": [
    "Return code only",
    "Keep output concise",
    "Ensure code is runnable"
  ],
  "template": "{system}\n\nRules:\n{rules}\n\nTask:\n{task}\n\nOutput:",
  "_purpose": "Default coding prompt template used when active profile is default.",
  "_comment_template": "Template string is formatted with runtime values (e.g., {task}, {system}, {rules})."
}
