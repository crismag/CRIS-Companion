Task: Add execution phase logging to engine.

Requirements:

Log each phase:
"Selecting module"
"Building prompt"
"Calling LLM"
"Processing result"
Logs must be structured and readable
Logs must integrate with UI control plane

Constraints:

No business logic change
No performance overhead
